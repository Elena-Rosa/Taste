@{
    Layout = "_Layout";
}

@model Taste.Models.Flavor

<h4>Add Flavor</h4>

<hr />
<p>
    @Html.ValidationMessageFor(model => model.Name)
</p>
@using (Html.BeginForm())
{
    @Html.Label("Name")
    @Html.TextBoxFor(model => model.Name, new { @class = "form-control" })

    @Html.Label("Description")
    @Html.TextBoxFor(model => model.Description, new { @class = "form-control" })


    <br>

    <input type="submit" value="Complete" class="btn btn-default" required="true" />
}

<p>@Html.ActionLink("back to flavors directory", "Index")</p>

<!-- @Html.Label("Treats")
@Html.DropDownList("TreatId", ViewBag.FlavorList as SelectList, string.Empty, new { @class = "form-control" }) -->

